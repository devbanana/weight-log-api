<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
         requireCoverageMetadata="true"
         beStrictAboutCoverageMetadata="true"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="KERNEL_CLASS" value="App\Infrastructure\Kernel" />
        <server name="SHELL_VERBOSITY" value="-1" />
    </php>

    <testsuites>
        <!-- Unit tests: Test domain objects in isolation (pure business logic) -->
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>

        <!-- Integration tests: Test adapters (projections, read models, API Platform resources, etc.) -->
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <!-- Commands and Handlers are tested via Behat use case tests, not PHPUnit -->
        <!-- Exceptions are simple DTOs with no logic worth unit testing -->
        <!-- Kernel is framework boilerplate (compiler passes are tested directly) -->
        <exclude>
            <directory>src/Application/*/Command</directory>
            <directory>src/Application/*/Query</directory>
            <directory>src/Domain/*/Event</directory>
            <directory>src/Domain/*/Exception</directory>
            <directory>src/Domain/Common/Exception</directory>
            <file>src/Infrastructure/Kernel.php</file>
        </exclude>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
        <bootstrap class="Symfony\Bridge\PhpUnit\SymfonyExtension">
            <parameter name="clock-mock-namespaces" value="App" />
            <parameter name="dns-mock-namespaces" value="App" />
        </bootstrap>
    </extensions>

</phpunit>
